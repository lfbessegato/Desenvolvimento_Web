<?php

    // operadores
    // == (Igual)
    // === (idêntico)
    // != ou <> (diferente)
    // !== (não identico)
    // < (menor)
    // > (maior)
    // <= (menor igual)
    // >= (maior igual)

    if(true) {
        echo 'sim, expressão é verdadeira';
    } else {
        echo 'não, expressão é falsa';
    }

?>