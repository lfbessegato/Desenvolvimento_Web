<?php

    // operadores de comparação
    // == (Igual)
    // === (idêntico)
    // != ou <> (diferente)
    // !== (não identico)
    // < (menor)
    // > (maior)
    // <= (menor igual)
    // >= (maior igual)

    //operadores lógicos
    // && ou AND (e)
    // || ou OR (ou)
    // XOR (xor)
    // ! (negação)

    if(true) {
        echo 'sim, expressão é verdadeira';
    } else {
        echo 'não, expressão é falsa';
    }

?>